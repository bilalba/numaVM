[Unit]
Description=NumaVM Setup (networking, hostname, SSH keys)
DefaultDependencies=no
After=local-fs.target
Before=ssh.service
Before=numavm-app.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/opt/numavm/setup.sh

[Install]
WantedBy=multi-user.target
